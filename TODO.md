Implement selection of workgroup size

Implement tool that measures which batch size and workgroup size is optimal for a FFT size

Fix hipFFT

Implement one big vector as input and output of fft instead of vector of vector to make one continous memory chuck in order to optimize DMA performace?

Fixed batch size?

